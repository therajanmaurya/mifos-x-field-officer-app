*.iml
.gradle
/local.properties
/.idea/workspace.xml
/.idea/libraries
.DS_Store
/build
/captures
.externalNativeBuild
.idea
/*.iml

# files for the dex VM
*.dex

# Java class files
*.class

# generated files
bin/
gen/
out/
build/
build-cache/
.externalNativeBuild
.cxx
# iOS — Xcode
# Per-user Xcode state (breakpoints, window positions, UI state, scheme
# selections) — never commit, it's per-developer and leaks personal data
# (macOS username) into every fork.
**/xcuserdata/
*.xcuserstate
# SwiftPM artifacts.
#
# `cmp-ios/KotlinMultiplatformLinkedPackage/` is TRACKED and must stay tracked: iosApp.xcodeproj
# declares it as an XCLocalSwiftPackageReference (relativePath = KotlinMultiplatformLinkedPackage),
# and `xcodebuild` resolves package dependencies BEFORE the Gradle build phase that would emit it —
# so on a fresh checkout the Package.swift + subpackage stubs must already be on disk or the build
# dies with "the package at '.../KotlinMultiplatformLinkedPackage' cannot be accessed".
#
# Only the RESOLVED artifacts below are ignored — they are the ~1.2 GB (dependency checkouts +
# SwiftPM index store) that a `git add -A` in a fork would otherwise stage as ~20k files and a
# >100 MB pack GitHub rejects.
cmp-ios/.build/
cmp-ios/KotlinMultiplatformLinkedPackage/.build/
cmp-ios/KotlinMultiplatformLinkedPackage/Package.resolved

# Eclipse project files
.classpath
.project

# Windows thumbnail db
.DS_Store

# IDEA/Android Studio project files, because
# the project can be imported from settings.gradle.kts
*.iml
.idea/*
!.idea/copyright
# Keep the code styles.
!/.idea/codeStyles
/.idea/codeStyles/*
!/.idea/codeStyles/Project.xml
!/.idea/codeStyles/codeStyleConfig.xml

# Kotlin
.kotlin

# Android Studio captures folder
captures/

/app/app-release.apk
app/app.iml
app/manifest-merger-release-report.txt

# Exclude Google services from prod flavour
cmp-android/src/prod/google-services.json

# Keystores - NEVER COMMIT THESE!
keystores/*.keystore
keystores/*.jks
*.keystore
*.jks

version.txt
fastlane/report.xml
secrets/android/firebaseAppDistributionServiceCredentialsFile.json
secrets/android/playStorePublishServiceCredentialsFile.json

# Secrets and Configuration - NEVER COMMIT THESE!
# secrets.env — REMOVED: that file is retired (2026-06-23). Keystore passwords
# now live in secrets/android/keystores/ (covered by secrets/ below).
# Phase 7 layout: secrets/live/ (REAL secrets, gitignored) + secrets/sample/ (committed
# fakes) + secrets/LAYOUT.yaml (the path contract). `secrets/*` (not `secrets/`) leaves the
# dir un-excluded so children can be re-included (git rule); live/ stays ignored via `*`.
secrets/*
!secrets/.gitkeep
# The secrets LAYOUT contract is the SoT for PATHS — holds NO secret values.
!secrets/LAYOUT.yaml
# Committed fake/sample secrets (structurally valid, obviously not real).
!secrets/sample
.env.local
!.env.local.example

# Fork identity — filled-in values. Committed schema: gradle/fork.properties.template
gradle/fork.properties

# Resolver-generated secret path map (build_secrets.rb#export-paths). Env-dependent
# (live-wins-else-sample), regenerated by CI / `/secrets pull` — never committed. The
# Gradle build reads it instead of shelling out to the resolver at configuration time.
gradle/secrets-paths.properties
PROJECT_SETUP_INFO.txt

# iOS Secrets - NEVER COMMIT THESE!
secrets/apple/match/.match_password
secrets/apple/appstore/AuthKey*.p8
secrets/ios/apn/APNAuthKey.p8
secrets/apple/match/match_ci_key
secrets/apple/match/match_ci_key.pub
fastlane/metadata/
fastlane/fastlane/metadata/
fastlane/screenshots/

# Screenshot PNGs — generated by scripts/generate-screenshots.sh from the committed HTML sources.
# The HTML files ARE committed (they're the source); only the rendered PNGs are transient.
deployment/*/metadata/**/*.png
deployment/*/metadata/**/screenshots/**/*.png

# Store-listing text + declarations + rendered media — DERIVED from app-profile/ by syncForkConfig
# (customization-surface owner:generated). app-profile/ is the SINGLE source of truth; the Fastfile
# `before_all` regenerates these from the SoT before every deploy, so they are NOT committed here.
# `**` covers all metadata roots (android/metadata, ios/appstore/metadata, desktop/mac-app-store/metadata,
# windows/metadata, linux/metadata). READMEs (*.md) stay tracked as docs.
deployment/**/metadata/**/*.txt
deployment/**/metadata/**/*.json
deployment/**/metadata/**/*.csv
deployment/**/metadata/**/*.png
# Ruby stuff we don't care about
.bundle/
vendor/
# …except the two bundler app-root configs, which pin BOTH roots at the SAME repo-root
# vendor tree so the gems are materialized ONCE. Must be tracked to help anyone but the
# machine that wrote them — untracked, a fresh clone gets no `path` at all and the roots
# diverge again. (Re-including needs the dir un-excluded first.)
!/.bundle/
/.bundle/*
!/.bundle/config
!deployment/.bundle/
deployment/.bundle/*
!deployment/.bundle/config
# …and the canary fixtures, which are only load-bearing if they survive a clone. RT-5/RT-6 assert
# these files exist AND are tracked, so an ignored fixture makes the RED/GREEN legs disagree between
# a developer's tree and CI — the canary would pass here and fail there for a reason nothing names.
!scripts/product-health/tests/**/.bundle/
!scripts/product-health/tests/**/.bundle/config

# Sync Log File
*.log
/build-logic/convention/secrets-backup/

# Backup files created by scripts
*.bak
.claude

# Room schemas are committed — required at build time for @AutoMigration
# (KSP needs the previous version's schema JSON to generate migration code).

# Security — NEVER commit encryption keys or secure storage
*.key
*.pem
.mifos-secure/
*.hprof

# Release state lives at workspace level (workspaces/{ws}/{project}/release-layer/), not in source.
release-layer/

# Generated fastlane reports (contain signing material) — never commit
deployment/fastlane/report.xml
deployment/fastlane/**/report.xml

# Phase 7: commit the obviously-fake secrets/sample/ placeholders even though the global
# *.keystore / *.key rules above would ignore them. (Real secrets live in gitignored secrets/live/.)
!secrets/sample/android/keystores/upload_keystore.keystore
!secrets/sample/desktop/linux/gpg_signing.key

# OG social images — DERIVED from app-profile/platforms/web/media/og-images (owner:generated)
deployment/**/og-images/**

# Per-machine store-listing drift cache — records the last-pushed listing hash per platform so
# deploy lanes skip re-uploading an unchanged listing. Derived/transient, never committed.
deployment/fastlane/.listing_sync_state.json

# Kotlin/Native SwiftPM dependency linking (KGP `swiftImport`) — DERIVED, do not commit.
#
# KGP synthesises a Swift package per native SPM dependency declared in a klib's metadata
# (GitLive Firebase pulls firebase-ios-sdk this way) so the linker can resolve it. What it
# emits is one Package.swift per Maven coordinate plus ZERO-BYTE .m/.h linker shims — all
# regenerated from the dependency graph on every iOS build, so committing them just adds
# churn on any version bump.
#
# The Package.resolved files are deliberately NOT ignored: they pin firebase-ios-sdk to a
# resolved commit and are the reproducibility guarantee for iOS dependencies.
.swiftpm-locks/**/Package.swift
.swiftpm-locks/**/*.m
.swiftpm-locks/**/*.h

# NOT ignored, deliberately: cmp-ios/KotlinMultiplatformLinkedPackage/**
#
# It looks like the same generated shim tree, but it is a COMMITTED INPUT. The Xcode project
# references it as an XCLocalSwiftPackageReference (relativePath = KotlinMultiplatformLinkedPackage)
# and resolves it at package-graph time, while Gradle generates into cmp-shared/build/kotlin/
# swiftImport + .swiftpm-locks/ and NEVER writes to cmp-ios/. Verified by moving the directory
# aside and re-running the generate tasks: it was not recreated. Ignoring it therefore breaks a
# fresh clone — Xcode cannot resolve the package before any Gradle task has run.
#
# It is also SHAPE-PINNED: the current KGP generators emit an umbrella that depends on a
# `KotlinMultiplatformLinkedPackageDylib` subpackage, whereas this committed copy depends on the
# dev_gitlive_* subpackages directly. Do not "refresh" it by copying the build output — that is a
# migration to a different linkage model, not a version bump.
