# Workspace library links (managed by /lib-integrate)
# Committed with .local=false so CI always resolves from Maven Central.
# This file is marked skip-worktree — local changes (flipping to .local=true
# for parallel dev) are ignored by git and never accidentally committed.
#
# To re-enable tracking for a real change (e.g. adding a library):
#   git update-index --no-skip-worktree lib-integrate.properties
#   <make changes, commit>
#   git update-index --skip-worktree lib-integrate.properties

# cmp-network-monitor workspace library link (managed by /lib-integrate)
cmp-network-monitor.local=false
cmp-network-monitor.path=../../../../mbs/kmp-toolkit/source/kmp-toolkit
cmp-network-monitor.module=:cmp-network-monitor
cmp-network-monitor.artifact=io.github.mobilebytelabs:cmp-network-monitor

# cmp-network-monitor-compose workspace library link (managed by /lib-integrate)
cmp-network-monitor-compose.local=false
cmp-network-monitor-compose.path=../../../../mbs/kmp-toolkit/source/kmp-toolkit
cmp-network-monitor-compose.module=:cmp-network-monitor-compose
cmp-network-monitor-compose.artifact=io.github.mobilebytelabs:cmp-network-monitor-compose

# kmp-product-flavors build-logic plugin link
kmp-product-flavors.local=false
kmp-product-flavors.path=../../../../mbs/kmp-product-flavors/source/kmp-product-flavors/build-logic
