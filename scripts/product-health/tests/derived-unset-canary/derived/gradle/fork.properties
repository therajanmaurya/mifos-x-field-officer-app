# Fixture: gradle/fork.properties exactly as scripts/white-label/derive.rb emits it on the TEMPLATE.
#
# derive.rb deliberately OMITS any app-profile value that is still a placeholder (derive.rb:61), so
# the signing keys are ABSENT here rather than carrying `YOUR_TEAM_ID`. Reading an absent key yields
# the empty string — and '^$' IS a declared placeholder for both signing categories
# (WHITE_LABEL_PLACEHOLDERS.yaml#apple_team_id / #apple_match_git_url).
app.id=org.mifos.kmp.template
org.name=Mifos Initiative
