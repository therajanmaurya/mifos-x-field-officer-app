package sample

/**
 * Registers every feature/*/di Koin module.
 *
 * The glob above opens a nested comment that never closes, so the compiler swallows everything
 * below and reports "Unclosed comment" at the end of the file.
 */
class Sample
