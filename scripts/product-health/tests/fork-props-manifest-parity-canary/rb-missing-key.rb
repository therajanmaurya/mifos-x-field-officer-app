# Fixture: Ruby manifest MISSING log.tag — the 2026-09-06 shape, where derive.rb produced a bridge
# without the per-flavor network/demo/log keys while syncForkConfig produced one with them.
  MAP = {
    "app.id"                             => "identity.app_id",
  }
