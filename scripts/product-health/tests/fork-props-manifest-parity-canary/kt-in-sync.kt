// Fixture: Kotlin manifest matching rb-in-sync.rb exactly.
        private val APP_PROFILE_MAP: Map<String, String> = mapOf(
            "app.id" to "identity.app_id",
            "log.tag" to "network.log_tag",
        )
