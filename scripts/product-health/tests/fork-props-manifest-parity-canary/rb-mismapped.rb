# Fixture: the same bogus mapping on the Ruby side, so the pair is in parity but BOTH wrong.
  MAP = {
    "app.id"                             => "identity.app_id",
    "log.tag"                            => "network.log_tag",
    "project.name"                       => "identity.project_name",
  }
